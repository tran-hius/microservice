version: "3.9"
services:
  backend:
    build: .
    ports: 
      - "3000:3000"
    depends_on:
      - db
    environment:
      DB_HOST: db
      DB_USER: hieutrandzz
      DB_PASSWORD: 0983310337
      DB_NAME: ecommerce

  db:
    image: mysql:8.0
    restart: always
    environment:
      MYSQL_ROOT_PASSWORD: 123456
      MYSQL_DATABASE: ecommerce
      MYSQL_USER: hieutrandzz
      MYSQL_PASSWORD: 0983310337
    ports:
      - "3306:3306"
    volumes:
      - db_data:/var/lib/mysql

volumes:
  db_data: